defaults:
  - api: default
  - data: default
  - model: lstm
  - run_name: null
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled
  - _self_

paths:
  models_dir: models
  outputs_dir: outputs

hydra:
  run:
    dir: .
  sweep:
    dir: .
  output_subdir: null
  job:
    chdir: false

force_refresh: False
run_name: null
target: level        # or spread
dataset: all_20200101_20251231
horizon: 24          # forecast horizon H